#include <iostream>

using namespace std;

int main() {
    
    float length;
    float breadth;
    float answer;
    
    cout << "Enter Length : ";
    cin >> length;
    cout << "Enter Breadth : ";
    cin >> breadth;
    
    cout << "Therefore Area is : " << (length*breadth);
    
    return 0;
}
